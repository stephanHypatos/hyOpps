# Copy this file to .env and fill in your credentials.
# .env is gitignored — never commit real secrets.

# ── Microsoft Azure / Teams ─────────────────────────────────────────────────
AZURE_TENANT_ID=your-directory-tenant-id
AZURE_CLIENT_ID=your-application-client-id
AZURE_CLIENT_SECRET=your-client-secret-value

# Microsoft 365 Group / Team ID for the team where partner users are added.
# Teams Admin Center → Teams → select team → Team ID
TEAMS_TEAM_ID=your-teams-group-id

# ── Metabase ─────────────────────────────────────────────────────────────────
# Base URL of your Metabase instance (no trailing slash)
METABASE_URL=https://your-metabase-instance.example.com
# API key from Metabase Admin → Settings → Authentication → API Keys (requires Metabase v0.46+)
METABASE_API_KEY=mb_your_api_key_here

# ── Email (SMTP) ──────────────────────────────────────────────────────────────
# Used by the "Share Documentation" onboarding step to email doc links to new users.
# Works with any SMTP provider (Gmail, Outlook, SendGrid SMTP relay, Mailgun, etc.)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=sender@example.com
SMTP_PASSWORD=your_smtp_password
# Optional: custom From header — defaults to SMTP_USER if not set
# EMAIL_FROM=HyOpps <noreply@example.com>

# ── Future integrations ──────────────────────────────────────────────────────
# SLACK_BOT_TOKEN=
# ATLASSIAN_URL=
# ATLASSIAN_API_TOKEN=
